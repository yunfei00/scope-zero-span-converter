# 版本变更记录

## v0.4.0（开发中）

### 方向调整

- 当前重点从继续优化 Zero Span 恢复精度，切换到原始时域波形研究、DCM SW 波形生成与参数提取。
- v0.1~v0.3 已验证的 Zero Span 算法作为稳定基线保留，v0.4 不主动修改其核心逻辑。

### 新增：波形研究 ROI

- 新增“波形研究”主页面。
- 在原始波形图中鼠标横向拖动选择 ROI。
- 支持通过数值精确设置 ROI 起点/终点。
- 显示 ROI 起止时间、时长、点数和原始索引。
- 支持放大到研究区域。
- 支持恢复完整波形视图后重新选择。
- 支持清除 ROI。
- 支持保存截取后的标准 `time_s,voltage_v` CSV。
- 可选保存 `.region.json`，记录 ROI 参数和来源。
- 可选将截取 CSV 的时间轴重新从 0 开始。
- ROI 改变后，下方 Zero Span 转换曲线自动联动刷新。
- 研究模式不重采样到原始 FSW Sweep Time，确保转换结果对应当前真实 ROI 时间范围。
- 新增 `waveform_research` JSON 配置段。
- 为后续 DCM SW 自动提取策略预留 `extraction_mode` 接口。
- 新增 ROI 裁剪、保存和联动转换自动测试。

### 新增：DCM SW 真值波形生成器

- 新增独立“DCM SW 生成器”页签，不需要加载 waveform 文件即可直接生成。
- 第一版采用单个 DCM 开关事件模型：上升沿 → 高电平导通 → 下降沿 → 续流低电平 → 断续阻尼谐振。
- 支持基线电压、开通高电平电压、续流低电平电压。
- 支持总显示时长、开关起始时间、导通时间、续流时间、上升沿时间、下降沿时间。
- 上升沿/下降沿时间允许设置为 `0 ns`，表示理想瞬时阶跃。
- 支持有符号上升沿尖峰和下降沿尖峰，正值表示向上、负值表示向下。
- 支持尖峰寄生振荡频率、尖峰衰减速率。
- 支持断续谐振初始振幅、断续谐振频率、断续谐振衰减速率。
- 支持示波器高斯底噪 RMS、采样率和固定随机种子。
- 主要参数采用滑块与数值框双向实时联动。
- 参数变化后可自动实时重新生成波形。
- 可保存合成 CSV 和参数真值 JSON。
- 支持重新加载已经保存的合成 CSV + 参数 JSON，恢复历史波形与参数。
- 合成 CSV 同时保存理想轨迹、尖峰/寄生振铃、断续谐振、噪声等真值分量。
- 参数 JSON 同时保存各阶段派生时间点，便于后续自动提取算法计算真实误差。
- 支持一键把合成波形直接发送到“波形研究”页进行 ROI 截取，无需先保存再加载。
- 默认只显示大的最终 SW 主波形，真值分量分析按需展开。
- 新增 DCM SW 阶段真值、随机噪声可重复性、参数 JSON、CSV 兼容性和混叠保护自动测试。

### 新增：DCM 基础参数反演

- 新增独立“DCM 参数提取”页签。
- 输入仅要求 `time_s,voltage_v`，不读取合成 CSV 中的真值分量，也不读取参数 JSON。
- 第一阶段自动提取：
  - 采样率与总显示时长；
  - 基线电压；
  - 开通高电平电压；
  - 续流低电平电压；
  - 开关起始时间；
  - 上升时间；
  - 导通时间；
  - 下降时间；
  - 续流时间；
  - 基线区估计噪声 RMS。
- 上升/下降时间同时显示“10%~90% 时间”和按当前半余弦边沿模型反解的完整边沿时间，避免指标口径混淆。
- 采用稳健中位数估计稳定电平、平滑导数粗定位主边沿、原始波形阈值穿越精定位、低通趋势定位续流结束点。
- 根据提取参数重建基础理想轨迹，并与 CSV 实测波形叠加显示。
- 可选显示 `实测 - 基础理想轨迹` 残差，为后续尖峰、寄生振铃与 DCM 谐振拟合提供输入。
- 主要参数提供初步置信度与总体置信度，低电平分离度不足、续流结束不明确、边沿采样点过少时给出提示。
- 支持保存参数提取结果 JSON。
- 新增默认参数、变化参数、0 ns 理想边沿、CSV 真值列隔离、GUI 残差显示等自动测试。
- GitHub Actions 补充 Linux Qt offscreen 所需系统库，恢复 GUI 自动测试兼容性。

### 兼容

- 继续保持 `schema_version = 1`。
- v0.1 / v0.2 / v0.3 JSON 没有 `waveform_research` 字段时自动使用默认值。
- v0.3 批量转换、配置模板、日志和最近使用状态继续保留。
- 当前 DCM 基础参数反演不修改 Zero Span 核心算法。

## v0.3.0

### 新增

- 单次转换 / 批量转换双页签 GUI。
- 批量递归扫描任务目录。
- 批量任务成功/失败表格。
- 每个任务独立输出目录。
- `batch_summary.csv` 和 `batch_summary.json`。
- 批量失败后继续执行选项。
- 客户配置模板保存、加载、删除、刷新。
- 用户模板目录：`~/ScopeZeroSpanConverter/templates/`。
- 应用日志：`~/ScopeZeroSpanConverter/logs/scope-zero-span-converter.log`。
- CLI `batch` 子命令。

### 兼容

- 保持 `schema_version = 1`。
- v0.1 / v0.2 JSON 缺失 v0.3 字段时自动使用默认值。
- 不修改 v0.2 已验证的 Zero Span 核心算法。

## v0.2.0

### 新增

- 导入 FSW Zero Span 实测 CSV。
- 示波器恢复曲线与 FSW 实测曲线叠加。
- MAE / RMSE / Bias / 最大绝对误差 / 相关系数。
- `comparison_to_fsw.csv`。
- `conversion_metadata.json`。
- Center / RBW / VBW 参数来源显示。
- FSW 对比自动测试。

## v0.1.0

### 首个基线版本

- 示波器时域波形 -> Zero Span 功率-时间曲线。
- Center 数字下变频。
- Gaussian RBW。
- RMS 功率检波。
- VBW 时域平滑。
- FSW Sweep Time / Points 重采样。
- JSON 参数加载/保存。
- PySide6 GUI。
- CLI。
- Matplotlib 中文字体适配。
- 200 MHz / Span 0 / RBW 10 MHz 算法基线测试。
- GitHub Actions 自动测试。
- `v*` Tag 自动构建 Windows ZIP 并创建 GitHub Release。
