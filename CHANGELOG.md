# 版本变更记录

## v0.4.0（开发中）

### 方向调整

- 当前重点从继续优化 Zero Span 恢复精度，切换到原始时域波形研究、DCM SW 波形生成与参数提取。
- v0.1~v0.3 已验证的 Zero Span 算法作为稳定基线保留，v0.4 不主动修改其核心逻辑。

### 新增：波形研究 ROI

- 新增“波形研究”主页面。
- 在原始波形图中鼠标横向拖动选择 ROI。
- 支持通过数值精确设置 ROI 起点/终点。
- 显示 ROI 起止时间、时长、点数和原始索引。
- 支持放大到研究区域。
- 支持恢复完整波形视图后重新选择。
- 支持清除 ROI。
- 支持保存截取后的标准 `time_s,voltage_v` CSV。
- 可选保存 `.region.json`，记录 ROI 参数和来源。
- 可选将截取 CSV 的时间轴重新从 0 开始。
- ROI 改变后，下方 Zero Span 转换曲线自动联动刷新。
- 研究模式不重采样到原始 FSW Sweep Time，确保转换结果对应当前真实 ROI 时间范围。
- 新增 `waveform_research` JSON 配置段。
- 为后续 DCM SW 自动提取策略预留 `extraction_mode` 接口。
- 新增 ROI 裁剪、保存和联动转换自动测试。

### 新增：DCM SW 真值波形生成器

- 新增独立“DCM SW 生成器”页签，不需要加载 waveform 文件即可直接生成。
- 第一版采用单个 DCM 开关事件模型：上升沿 → 高电平导通 → 下降沿 → 续流低电平 → 断续阻尼谐振。
- 支持基线电压、开通高电平电压、续流低电平电压。
- 支持总显示时长、开关起始时间、导通时间、续流时间、上升沿时间、下降沿时间。
- 上升沿/下降沿时间允许设置为 `0 ns`，表示理想瞬时阶跃。
- 支持有符号上升沿尖峰和下降沿尖峰，正值表示向上、负值表示向下。
- 支持尖峰寄生振荡频率、尖峰衰减速率。
- 支持断续谐振初始振幅、断续谐振频率、断续谐振衰减速率。
- 支持示波器高斯底噪 RMS、采样率和固定随机种子。
- 主要参数采用滑块与数值框双向实时联动。
- 参数变化后可自动实时重新生成波形。
- 可保存合成 CSV 和参数真值 JSON。
- 支持重新加载已经保存的合成 CSV + 参数 JSON，恢复历史波形与参数。
- 合成 CSV 同时保存理想轨迹、尖峰/寄生振铃、断续谐振、噪声等真值分量。
- 参数 JSON 同时保存各阶段派生时间点，便于后续自动提取算法计算真实误差。
- 支持一键把合成波形直接发送到“波形研究”页进行 ROI 截取，无需先保存再加载。
- 默认只显示大的最终 SW 主波形，真值分量分析按需展开。
- 新增 DCM SW 阶段真值、随机噪声可重复性、参数 JSON、CSV 兼容性和混叠保护自动测试。

### 新增：DCM 基础参数反演

- 新增独立“DCM 参数提取”页签。
- 输入仅要求 `time_s,voltage_v`，不读取合成 CSV 中的真值分量，也不读取参数 JSON。
- 第一阶段自动提取：
  - 采样率与总显示时长；
  - 基线电压；
  - 开通高电平电压；
  - 续流低电平电压；
  - 开关起始时间；
  - 上升时间；
  - 导通时间；
  - 下降时间；
  - 续流时间；
  - 基线区估计噪声 RMS。
- 上升/下降时间同时显示“10%~90% 时间”和按当前半余弦边沿模型反解的完整边沿时间，避免指标口径混淆。
- 采用稳健中位数估计稳定电平、平滑导数粗定位主边沿、原始波形阈值穿越精定位、低通趋势定位续流结束点。
- 根据提取参数重建基础理想轨迹，并与 CSV 实测波形叠加显示。
- 可选显示 `实测 - 基础理想轨迹` 残差，为后续尖峰、寄生振铃与 DCM 谐振拟合提供输入。
- 主要参数提供初步置信度与总体置信度，低电平分离度不足、续流结束不明确、边沿采样点过少时给出提示。
- 支持保存参数提取结果 JSON。
- 新增默认参数、变化参数、0 ns 理想边沿、CSV 真值列隔离、GUI 残差显示等自动测试。
- GitHub Actions 补充 Linux Qt offscreen 所需系统库，恢复 GUI 自动测试兼容性。

### 新增：开关沿尖峰与寄生振铃反演

- 第二阶段从第一阶段 `实测 - 基础理想轨迹` 残差继续提取开关沿寄生参数。
- 自动提取上升沿有符号初始尖峰电压和下降沿有符号初始尖峰电压。
- 当前按生成模型假设，上升沿与下降沿共享寄生振铃频率和指数衰减速率。
- 上升沿与下降沿允许独立拟合相位，避免因边界定位、采样相位或真实寄生网络相位差影响幅度与频率估计。
- 频率先通过局部 FFT 粗估，再使用共享频率/共享衰减、独立幅度/相位的双边沿联合网格拟合精修。
- 不新增 SciPy 依赖，保持 NumPy/Pandas/PySide6 打包基线。
- 结果页新增上升尖峰、下降尖峰、共享振铃频率、共享衰减速率、拟合相位和局部 R²。
- 主图新增“基础 + 尖峰/寄生振铃拟合”轨迹。
- 残差视图同时显示基础残差、尖峰/寄生振铃拟合分量以及扣除开关沿后的残差。
- 参数 JSON 导出升级为基础结果 + 开关沿振铃结果的分阶段结构。
- 当尖峰接近底噪、局部周期数不足或拟合 R² 较低时给出低置信度提示。
- 新增默认 60 MHz / 8 µs⁻¹、变化 85 MHz / 12 µs⁻¹、0 尖峰以及 GUI 展示真值回归测试。

### 新增：DCM 断续谐振与最终噪声反演

- 第三阶段从“扣除基础轨迹与上下沿尖峰/寄生振铃后的残差”继续提取 DCM 断续区参数。
- 自动提取 DCM 断续谐振初始振幅、谐振频率和指数衰减速率。
- DCM 局部模型允许独立相位，用于吸收断续起点定位和采样相位的轻微偏差。
- 先使用保留瞬态起始能量的 FFT 粗估频率，再通过频率/衰减二维网格拟合精修。
- 第三阶段仍不新增 SciPy 依赖。
- 主图新增“基础 + 尖峰/振铃 + DCM 完整拟合”轨迹。
- 残差视图新增 DCM 断续谐振拟合分量，以及所有已解释分量扣除后的最终残差。
- 最终残差使用 robust RMS 估计示波器底噪，并同时给出整体 RMSE 作为模型误差参考。
- 参数 JSON 导出升级为 `dcm_parameter_identification_v3`，包含 basic、edge_ringing、discontinuous_resonance 三层结果。
- 当 DCM 周期数不足、初始振幅接近底噪或拟合 R² 偏低时给出低置信度提示。
- 新增默认 DCM 参数、变化频率/衰减/幅度、零 DCM 分量和 GUI 第三阶段展示自动测试。

### 新增：DCM 全局联合参数精修

- 第四阶段以前三阶段的可解释提取结果作为初值，对完整 DCM 模型进行有界联合精修。
- 非线性联合优化参数包括：开关起始时间、上升时间、导通时间、下降时间、续流时间、寄生振铃频率/衰减、DCM 谐振频率/衰减。
- 在每个非线性候选点上，基线/高电平/续流低电平以及上升沿、下降沿、DCM 三组阻尼振铃的正交幅度系数通过加权线性最小二乘直接求解。
- 上升沿、下降沿和 DCM 瞬态区在优化目标中提高权重，避免长时间稳定区掩盖关键瞬态特征。
- 使用分阶段结果附近的有限搜索范围，降低真实波形中过拟合和参数无约束漂移风险。
- 大波形优化阶段采用分层抽样，优先保留边沿和 DCM 区高密度样本；最终结果仍回到完整时间轴重建。
- 不新增 SciPy 依赖，继续使用 NumPy 实现有界坐标搜索和线性最小二乘。
- “DCM 参数提取”页面新增手动 `运行全局联合精修（较慢）` 按钮；前三阶段保持自动执行，避免加载大 CSV 时自动进入重优化。
- 联合结果保留前三阶段原始结果，同时新增联合精修后的全套推荐参数、优化前 RMSE、优化后 RMSE、RMSE 改善百分比、完整波形 R²、最终残差 robust RMS、迭代轮数和评估次数。
- 主图新增“全局联合精修重建波形”，残差视图新增“联合精修最终残差”。
- 参数 JSON 导出升级为 `dcm_parameter_identification_v4`，新增 `global_refinement` 分层结果。
- 当联合优化改善很小、达到迭代上限或最终残差明显高于基线噪声时给出研究提示，而不是静默覆盖分阶段结果。
- 新增默认参数、变化参数、结果序列化和 GUI 全局精修自动测试。

### 兼容

- 继续保持 `schema_version = 1`。
- v0.1 / v0.2 / v0.3 JSON 没有 `waveform_research` 字段时自动使用默认值。
- v0.3 批量转换、配置模板、日志和最近使用状态继续保留。
- 当前 DCM 参数反演和全局精修均不修改 Zero Span 核心算法。

## v0.3.0

### 新增

- 单次转换 / 批量转换双页签 GUI。
- 批量递归扫描任务目录。
- 批量任务成功/失败表格。
- 每个任务独立输出目录。
- `batch_summary.csv` 和 `batch_summary.json`。
- 批量失败后继续执行选项。
- 客户配置模板保存、加载、删除、刷新。
- 用户模板目录：`~/ScopeZeroSpanConverter/templates/`。
- 应用日志：`~/ScopeZeroSpanConverter/logs/scope-zero-span-converter.log`。
- CLI `batch` 子命令。

### 兼容

- 保持 `schema_version = 1`。
- v0.1 / v0.2 JSON 缺失 v0.3 字段时自动使用默认值。
- 不修改 v0.2 已验证的 Zero Span 核心算法。

## v0.2.0

### 新增

- 导入 FSW Zero Span 实测 CSV。
- 示波器恢复曲线与 FSW 实测曲线叠加。
- MAE / RMSE / Bias / 最大绝对误差 / 相关系数。
- `comparison_to_fsw.csv`。
- `conversion_metadata.json`。
- Center / RBW / VBW 参数来源显示。
- FSW 对比自动测试。

## v0.1.0

### 首个基线版本

- 示波器时域 waveform -> Zero Span 功率-时间曲线。
- Center 数字下变频。
- Gaussian RBW。
- RMS 功率检波。
- VBW 时域平滑。
- FSW Sweep Time / Points 重采样。
- JSON 参数加载/保存。
- PySide6 GUI。
- CLI。
- Matplotlib 中文字体适配。
- 200 MHz / Span 0 / RBW 10 MHz 算法基线测试。
- GitHub Actions 自动测试。
- `v*` Tag 自动构建 Windows ZIP 并创建 GitHub Release。
